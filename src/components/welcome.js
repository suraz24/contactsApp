"use strict";

import React from "react";

const Home = React.createClass({
    render: function () {
        return (
            <div className="jumbotron">
                <h1>Contacts</h1>
                <p> Click on the contacts to view more details about them.</p>
            </div>
        );
    }
});

export default Home;